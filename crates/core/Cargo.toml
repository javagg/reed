[package]
name = "reed-core"
version = "0.1.0"
edition = "2021"
description = "Core abstractions for the Reed finite element discretization library"

[dependencies]
thiserror = { workspace = true }
num-traits = { workspace = true }
